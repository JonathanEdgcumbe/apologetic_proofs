<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:fillViewport="true"
    android:layout_marginStart="5sp"
    android:scrollbars = "vertical"
    android:scrollbarStyle="insideInset">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <Button
            style="@style/MainBtn"
            android:text="Omnipotence (All-powerful)"
            android:id="@+id/OmnipotenceBtn"/>

        <Button
            style="@style/MainBtn"
            android:text="Omniscience (All-knowing)"
            android:id="@+id/OmniscienceBtn"/>

        <Button
            style="@style/MainBtn"
            android:text="Omnibenevolence (All-good)"
            android:id="@+id/OmnibenevolenceBtn"/>

        <Button
            style="@style/MainBtn"
            android:text="Omnipresence (All-present)"
            android:id="@+id/OmnipresenceBtn"/>

        <Button
            style="@style/MainBtn"
            android:text="Personal"
            android:id="@+id/PersonalBtn"/>

        <Button
            style="@style/MainBtn"
            android:text="Spirit"
            android:id="@+id/SpiritBtn"/>

        <Button
            style="@style/MainBtn"
            android:text="Eternal"
            android:id="@+id/EternalBtn"/>
        <Button
            style="@style/MainBtn"
            android:text="Back"
            android:id="@+id/BackButton"/>

    </LinearLayout>
</ScrollView>
